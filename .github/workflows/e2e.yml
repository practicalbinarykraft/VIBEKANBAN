name: CI

on:
  push:
    branches: [main, "claude/**"]
  pull_request:

jobs:
  unit:
    name: Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Run unit tests
        run: npm run test:unit

  e2e:
    name: E2E Tests
    needs: unit
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Debug env before build
        run: |
          echo "AUTOPILOT flags:"
          echo "  FEATURE_AUTOPILOT_V2=$FEATURE_AUTOPILOT_V2"
          echo "  NEXT_PUBLIC_FEATURE_AUTOPILOT_V2=$NEXT_PUBLIC_FEATURE_AUTOPILOT_V2"
        env:
          FEATURE_AUTOPILOT_V2: "1"
          NEXT_PUBLIC_FEATURE_AUTOPILOT_V2: "1"

      - name: Build for production
        run: npm run build
        env:
          PLAYWRIGHT: "1"
          FEATURE_AUTOPILOT_V2: "1"
          NEXT_PUBLIC_FEATURE_AUTOPILOT_V2: "1"

      - name: Debug build output
        run: ls -la .next | head -50

      - name: Run E2E tests (deterministic)
        env:
          PLAYWRIGHT: "1"
          FEATURE_AUTOPILOT_V2: "1"
          NEXT_PUBLIC_FEATURE_AUTOPILOT_V2: "1"
          DEBUG: "pw:webserver,pw:api"
        run: npx playwright test --trace on-first-retry --reporter html --workers=1

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report
          if-no-files-found: ignore

      - name: Upload test-results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results
          if-no-files-found: ignore
